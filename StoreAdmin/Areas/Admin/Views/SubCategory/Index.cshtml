@model Tuple<MainCategory,Category,List<SubCategory>>


<div class="row">
    <div class="btn-group">
        <a class="btn btn-primary" asp-controller="SubCategory" asp-action="Create" asp-route-id="@Model.Item2.Id" style="max-width: 200px;">
            <i class="bi bi-plus-circle-fill"></i>
            Yeni Kategori
        </a>
        <a class="btn btn-outline-primary" asp-action="Index" asp-controller="Category" asp-route-id="@Model.Item1.Id" style="max-width: 200px;">
            <i class="bi bi-house-up"></i>
            Üst Kategoriye Git
        </a>
    </div>
    <h3><mark>@Model.Item1.Name.ToUpper() > @Model.Item2.Name.ToUpper()</mark>  Alt Kategorileri  </h3>

    <div class="col-sm-12 col-md-12 col-xl-12">
        <table id="myTable" class="table table-bordered responsive nowrap" width="100%">

            <thead>
                <tr>

                    <th>#</th>
                    <th>Alt Kategori Adı</th>
                    <th>Ürünlere Git</th>
                    <th>Işlem</th>

                </tr>
            </thead>
            <tbody>
                @foreach (var SCate in Model.Item3)
                {
                    <tr>
                        <td>@SCate.Id</td>
                        <td>@SCate.Name</td>

                        <td><a href="#">Ürünlere Git</a><sub>(99)</sub></td>
                        <td>
                            <div class="btn-group">
                                <a asp-controller="SubCategory" asp-action="Update" asp-route-id="@SCate.Id" class="btn btn-success">Güncelle</a>
                                <a asp-controller="SubCategory" asp-action="Delete" asp-route-id1="@SCate.Id" asp-route-id2="@Model.Item2.Id" onclick="return confirm('Kategori Silinsin mi ?')" class="btn btn-outline-danger">Sil</a>
                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>

    </div>
    <!-- Button trigger modal -->
    <!-- Modal -->

</div>